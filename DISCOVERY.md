# Discovery: organvm/dot-github--poiesis

`organvm/dot-github--poiesis` has real latent value as the ORGAN-II GitHub membrane: it is the already-live `.github` defaults repository that exports the public organization profile, inherited community health files, artistic/research issue intake templates, PR template, reusable CI/release workflow templates, dispatch receiver, `seed.yaml` automation contract, and Poiesis aesthetic policy for every repository in the creative organ. Its highest value is not direct product revenue by itself, but leverage: it can become the reusable onboarding, governance, and collaborator-intake bundle that lets ORGAN-II projects present a coherent public face to artists, researchers, grant reviewers, contributors, and downstream ORGAN-IV/V distribution flows while giving the rest of the estate a pattern for machine-checked org defaults. Promote it as infrastructure; the single best concrete first task is to add an `org-defaults.manifest.json` plus validation tests that enumerate every exported GitHub surface, parse every YAML/JSON template, assert required community/profile/template files exist, and produce a small sync-ready report so ORGAN-IV can compare, copy, or enforce this defaults bundle across other organs without re-discovering static files by hand.
